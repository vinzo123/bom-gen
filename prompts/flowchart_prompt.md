You are helping convert a client requirement PDF into a clean Mermaid flowchart.

Rules:
- Return only Mermaid code.
- Use `flowchart TD`.
- Keep node names short and business-friendly.
- Show the main actor/process flow first.
- Use decision diamonds for approvals, validations, errors, and yes/no checks.
- Do not invent company-specific details that are not present in the text.
- If the PDF text is unclear, create a high-level flow instead of guessing too much.

Client PDF text:

```text
{pdf_text}
```

